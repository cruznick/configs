{{- /*
  Shared template for work git identity fragments.
  Usage: {{ template "gitconfig_work_identity" dict "slug" "acme" "platform" "gl" "email" "user@acme.com" "name" "Full Name" }}
*/ -}}
[user]
  name       = {{ .name | quote }}
  email      = {{ .email | quote }}
  signingkey = ~/.ssh/signing-pubs/work-{{ .platform }}.pub
