# companies/example — Work company template

This directory contains template files for a work company setup.
It is **not deployed by chezmoi** — it is a reference and used by `work-config add`.

## Files

- `.envrc.example` — direnv + direnv-1password template for per-company secrets.
  Copy to `~/repos/work/<platform>/<slug>/.envrc.example` (done automatically by `work-config add`).
  Then: `cp .envrc.example .envrc`, fill in `op://` references, `direnv allow`.

- `npmrc` — private npm registry config (no credentials — those come from direnv `.envrc`)

- `secrets.zsh.tmpl` — legacy: per-company shell env stubs (superseded by `.envrc.example` + direnv)

- `README.md` — this file

## Secrets architecture

| What | Where | Why |
|------|-------|-----|
| Registry tokens, proxies | `.envrc` (per company repo dir) | Fetched from 1Password on `cd`; unloaded on `cd` out |
| Global tokens (`GITHUB_TOKEN`) | `~/.secrets.zsh` | Loaded once per shell session |
| Non-secret env vars | `~/.zshrc.d/work-<slug>.zsh` | Generated by chezmoi run_once_40 |
