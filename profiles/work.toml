# Generic support mode only.
# Concrete work identity still comes from local work-context files.
[profile]
name = "work"

[providers]
enabled = ["gh", "gl"]
default = "gh"

[cli]
glab_config_dir = "glab-work"
