#!/usr/bin/env bash
set -euo pipefail
cat <<'EOF'
work-config is deprecated.

Use local-only work contexts instead:
  dots-work create
  dots-work list
  dots-work remove --slug=<slug>

Concrete work details now live in:
  ~/.config/dotfiles/work-contexts/*.toml
EOF
