# Managed by chezmoi.
#
# Profile/provider selection and machine-specific overrides now live in:
#   ~/.config/dotfiles/overrides.toml
#
# Legacy top-level [data] values are no longer required for baseline bootstrap.
# Persist sourceDir so plain `chezmoi apply` continues to work after
# `chezmoi init --source ...`.
sourceDir = {{ .chezmoi.sourceDir | quote }}

[data]
