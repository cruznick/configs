# Legacy directory — historical configs, not managed by chezmoi
legacy/**
legacy/

# Fonts — installed to ~/Library/Fonts/ by run_once_05, not deployed to ~/font/
font/
font/**

# Documentation only
docs/**
docs/

# Manual app-export artifacts — reference only, never deployed by chezmoi
apps/**
apps/

# Local work-context helper examples — not deployed directly
companies/**
companies/

# Repo-tracked profile data — sourced by templates, not deployed directly
profiles/**
profiles/

# Personal identity envrc examples — reference only, not deployed
personal/**
personal/

# chezmoi internal — these special dirs are handled automatically, not ignored
.chezmoidata/**
.chezmoitemplates/**
.chezmoiignore
.chezmoi.toml.tmpl

# Repo files
install.sh
README.md
LICENSE

# macOS
.DS_Store
